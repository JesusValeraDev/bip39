build:
  environment:
    node: '16'
  
  nodes:
    analysis:
      tests:
        override:
          - js-scrutinizer-run
    
    coverage:
      dependencies:
        override:
          - npm install
      tests:
        override:
          - command: npm run test:coverage
            coverage:
              file: coverage/clover.xml
              format: clover

checks:
  javascript: true

filter:
  excluded_paths:
    - 'node_modules/*'
    - 'dist/*'
    - 'coverage/*'
    - 'test/*'
    - '*.config.*'
    - '*.d.ts'

coding_style:
  javascript: true

